[project]
name = "chromium-hardening-guide"
version = "1.0.0"
description = ""
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "zensical>=0.0.50",
]
